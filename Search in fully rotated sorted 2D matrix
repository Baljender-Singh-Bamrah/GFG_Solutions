class Solution {
  public:
    bool searchMatrix(vector<vector<int>> &mat, int x) {
        // code here
        for(auto &it: mat) {
            for(auto &k: it) {
                if(k==x) {
                    return true;
                }
            }
        }
        return false;
    }
};
